# Server-only secrets — never in the repo or the client bundle (spec §11.7).
# Copy to .env.local for local dev. Vite only exposes VITE_-prefixed vars to the client.
ANTHROPIC_API_KEY=
COMPANION_MODEL=claude-haiku-4-5
CONFIDENCE_FLOOR=0.55

# Client flags
VITE_DEMO_MODE=false
